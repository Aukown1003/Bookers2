<h1>books_index</h1>

<!--book-index部分-->
<% @books.each do |book| %>
<div>
  <p>投稿ユーザー画像：<%= image_tag book.user.get_profile_image(100,100) %></p>
  <p>ショップ名：<%= book.title %></p>
  <p>説明：<%= book.body %></p>
</div>
<% end %>

